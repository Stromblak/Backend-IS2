instalar docker

docker pull mysql			# descargar la imagen, si no la tienen
docker-compose up -d 		# iniciar la base de datos
docker-compose down			# detenerla

Pueden acceder en localhost:8080, las credenciales estan en ../.env
